# developer.clashofclans.com 發的 API key。
# 注意：key 綁定建立時指定的來源 IP（CIDR），伺服器公網 IP 一變就全部 403。
COC_API_KEY=

# SQLite 位置
DB_PATH=data/coc-cards.db

# 本機 http://localhost 開發時設 0，否則瀏覽器會丟掉 cookie。正式站不要設。
# COOKIE_SECURE=0
